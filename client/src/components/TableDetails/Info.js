import React from 'react'
import PropTypes from 'prop-types'

const Info = ({description}) => {
    return (
        <div>
            <p>{description}</p>
        </div>
    )
}

Info.propTypes = {

}

export default Info
